runtime: nodejs22
service: backend

env_variables:
  DB_HOST: '34.172.113.167'
  DB_USER: 'admin'
  DB_PASS: 'mypassword'
  DB_NAME: 'notes_ 123230187'

handlers:
- url: /.*
  script: auto

automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 0
  max_instances: 1